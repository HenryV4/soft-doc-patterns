{
  "dependencies": {
    "csv-parser": "^3.2.0",
    "mysql2": "^3.19.1",
    "reflect-metadata": "^0.2.2",
    "sqlite3": "^5.0.2",
    "tsyringe": "^4.10.0",
    "typeorm": "^0.3.6"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "generate-csv": "ts-node src/utils/CsvGenerator.ts",
    "import-data": "ts-node src/index.ts",
    "dev": "ts-node src/index.ts",
    "clean": "powershell -Command \"if (Test-Path database.sqlite) { Remove-Item database.sqlite }; if (Test-Path data/*.csv) { Remove-Item data/*.csv }\"",
    "full-run": "npm run clean && npm run generate-csv && npm run import-data"
},
  "devDependencies": {
    "rimraf": "^6.1.3"
  }
}
